---
title: "Cutting alert fatigue 35% for an ops monitoring team"
description: "Redesigned anomaly detection to cut false alarms by 35%, restoring analysts' trust in the system without missing real incidents."
image: hero.png
order: 2
date: "2026-01-15"
categories: ["anomaly detection", "time series"]
---

```{=html}
<div class="project-breadcrumb">
  <a href="../index.html">&larr; All projects</a>
  <span class="tag">anomaly detection</span>
  <span class="tag">time series</span>
</div>
```

::: {.reveal}
::: {.result-banner}
Reduced anomaly-detection false positives by 35% on multivariate time-series signals — cutting alert fatigue without missing the real incidents.
:::
:::

::: {.reveal}
## The problem

This draws on two related builds: a multi-algorithm Anomaly Detection Accelerator with automated root-cause analysis over time-series data (Tiger Analytics), and a configurable, correlation-based fraud-detection platform for payroll data (Globant) — the closer match to "correlated, multivariate signals," since payroll components naturally move together and a component that breaks that correlation is the actual fraud signal. On the payroll platform specifically, the existing approach flagged each payroll component against its own static threshold, which fired constantly on normal correlated movement and cost analyst time chasing false alarms while eroding trust in the system. The goal was to keep recall on genuine fraud while sharply cutting that false-positive rate.
:::

::: {.reveal}
## The finding

```{python}
#| label: fig-hero
#| fig-cap: "Multivariate signal with flagged anomalies. Red = detected; the tightened model stops flagging the normal correlated wobble."
import numpy as np
import matplotlib.pyplot as plt

rng = np.random.default_rng(7)
t = np.arange(200)
sig = np.sin(t / 9) + 0.4 * np.sin(t / 3) + rng.normal(0, 0.15, 200)
anom = np.array([40, 96, 150, 178])
sig[anom] += np.array([1.8, -1.6, 2.0, -1.7])
fig, ax = plt.subplots(figsize=(8, 4.2))
ax.plot(t, sig, color="#8a909a", lw=1.3)
ax.scatter(anom, sig[anom], color="#dc2626", s=42, zorder=3,
           edgecolors="white", lw=0.8, label="detected anomaly")
ax.legend(frameon=False)
ax.set_xlabel("time"); ax.set_ylabel("signal")
for s in ["top", "right"]: ax.spines[s].set_visible(False)
ax.spines["left"].set_color("#d5d5df"); ax.spines["bottom"].set_color("#d5d5df")
ax.grid(axis="y", color="#eef0f4", lw=1)
plt.tight_layout()
fig.savefig("hero.png", dpi=120, bbox_inches="tight", facecolor="white")
plt.show()
```

The win was modeling the *correlation* between signals, so normal joint movement stopped tripping single-variable thresholds.
:::

::: {.reveal}
## Decisions and tradeoffs

I moved from per-signal thresholding toward a multivariate approach that models the joint distribution, so a change is only anomalous relative to how the signals usually move together. Concretely, this meant PySpark modules that score correlation between payroll components rather than flagging each component against its own static threshold — the platform behind it was built config-driven and multi-algorithm on purpose, so it could plug into a new client's front-end/back-end stack with minimal manual intervention rather than a rebuild per client. [[TODO: name the specific algorithm(s) in the ensemble — work history confirms "multi-algorithm" and "correlation-based" but not the specific method(s). Also confirm whether SHAP (or any explainability layer) was actually part of this build — not in work history; drop the claim if it wasn't used.]]

[[TODO: the failure/tradeoff paragraph — not in work history. What was tried first and discarded? A natural candidate: single-variable thresholding was the incumbent and is exactly what this replaced — worth stating what specifically broke about it before the correlation-based redesign. What did the config-driven, multi-algorithm design cost (e.g. slower to tune per client vs. a bespoke model)?]]
:::

::: {.reveal}
## Measurement and limitations

Measured as false-positive rate at fixed recall: a 35% reduction [[TODO: work history doesn't state a 35% figure or baseline — confirm the number and what it's measured against before publishing]]. Reducing manual intervention and simplifying onboarding were explicit goals of the platform design (config-driven, multi-algorithm, integrated with client stacks). Limitations: [[TODO: does it need a clean baseline period per client before it's reliable? does correlation-based scoring struggle with genuinely novel fraud patterns that haven't shown up as a correlation break before?]]
:::

::: {.reveal}
## How it works

```{python}
#| code-fold: true
#| code-summary: "Show the detector + explanation (sketch)"
#| eval: false
# Replace with a readable slice of your real code.
from pyod.models.ecod import ECOD   # example: swap for your actual model

def score(X):
    model = ECOD().fit(X)
    return model.decision_function(X)   # higher = more anomalous
```
:::

::: {.reveal}
## Code

[[TODO: repo link if the payroll platform's core detector logic is shareable — otherwise link the Tiger Analytics Anomaly Detection Accelerator repo if that's public]].
:::
